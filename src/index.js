import React from "react";
import ReactDOM from "react-dom";
import App from "./compp/App"; // Import the Header component

ReactDOM.render(<App />, document.getElementById("root")); // Render the Header component
